[tox]
env_list = py
skip_missing_interpreters = false

[testenv]
allowlist_externals = bash
# expect parent tox to select correct python executable
command = bash -c '[ "$(python -c "import sys; print(sys.executable)")" = "$(py bin --path {env:EXPECTED_PY_TAG})" ]'
